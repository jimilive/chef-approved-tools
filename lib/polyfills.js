// Empty polyfills file for modern browsers
// This replaces Next.js default polyfills to reduce bundle size

// Only include essential polyfills that we actually need
// Modern browsers (Chrome 91+, Firefox 90+, Safari 14+) don't need these

export default function() {
  // No polyfills needed for modern browsers
}